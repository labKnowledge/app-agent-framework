{
  "name": "@app-agent/state-manager",
  "version": "0.1.0",
  "type": "module",
  "description": "App state integration and tracking for App-Agent",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@app-agent/entities": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
